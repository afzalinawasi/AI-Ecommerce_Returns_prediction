# AI-Ecommerce_Returns_prediction
A Streamlit-based AI/ML app that predicts ecommerce order return risk using supervised classification. The app uses a trained Random Forest model to show return probability, risk level, and business recommendations.
